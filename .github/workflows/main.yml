name: Run Adaptive NN Assignment

on:
  push:
    branches:
      - main

jobs:
  run-assignment:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Run assignment script
        run: |
          python code/adaptive_assignment_1.py <<EOF
          3
          1
          -2
          -1
          -4
          0
          1
          0
          EOF
